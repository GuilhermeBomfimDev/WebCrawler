using HtmlAgilityPack;
using WebCrawler.Models;

namespace WebCrawler.Services
{
    class ExtractorService
    {
        public async Task<List<DadosProxy>> ExtractProxy(string utrl)
        {
            var proxies = new List<DadosProxy>();

            return proxies;
        }
    }
}
